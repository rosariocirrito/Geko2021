package it.sicilia.regione.gekoddd.geko.programmazione.domain.azioneAssegnazione;

import org.springframework.util.StringUtils;
import org.springframework.validation.Errors;

import it.sicilia.regione.gekoddd.geko.programmazione.domain.azione.Azione;

/**
 * <code>Validator</code> for <code>Pet</code> forms.
 *
 * @author Ken Krebs
 * @author Juergen Hoeller
 */
public class AzioneAssegnazioneValidator {

	public void validate(AzioneAssegnazione azioneAssegnazione, Errors errors) {
            /*
		if (!StringUtils.hasLength(azione.getDenominazione())) {
			errors.rejectValue("denominazione", "required", "required");
		}
		if (!StringUtils.hasLength(azione.getDescrizione())) {
			errors.rejectValue("descrizione", "required", "required");
		}
                if (!StringUtils.hasLength(azione.getProdotti())) {
			errors.rejectValue("prodotti", "required", "required");
		}
                * 
                */
                
	}

 
}