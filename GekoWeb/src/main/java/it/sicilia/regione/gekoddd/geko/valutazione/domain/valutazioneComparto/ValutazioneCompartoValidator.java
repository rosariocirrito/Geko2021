package it.sicilia.regione.gekoddd.geko.valutazione.domain.valutazioneComparto;


import org.springframework.validation.Errors;

/**
 * <code>Validator</code> for <code>Pet</code> forms.
 *
 * @author rc
 */
public class ValutazioneCompartoValidator {

	public void validate(ValutazioneComparto valutazione, Errors errors) {
		// da completare
        /*
            if (valutazione.getAnalProgrAss()<5 || valutazione.getAnalProgrAss()>10) {
			errors.rejectValue("Capacit� di Analisi e Programmazione", "deve essere tra 5 e 10", "deve essere tra 5 e 10");
		}
            if (valutazione.getRelazCoordAss()<5 || valutazione.getRelazCoordAss()>10) {
			errors.rejectValue("Capacit� di Relazione e Coordinamento", "deve essere tra 5 e 10", "deve essere tra 5 e 10");
		}
            if (valutazione.getGestRealAss()<5 || valutazione.getGestRealAss()>10) {
			errors.rejectValue("indicazioni", "required", "required");
		}
            
		if (!StringUtils.hasLength(azione.getDenominazione())) {
			errors.rejectValue("denominazione", "required", "required");
		}
		
                if (!StringUtils.hasLength(azione.getProdotti())) {
			errors.rejectValue("prodotti", "required", "required");
		}
                * 
                */
                
	}

 
}